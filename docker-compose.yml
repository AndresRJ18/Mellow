services:
  backend:
    build: ./backend
    expose:
      - "8000"
    env_file: .env
    restart: unless-stopped
    networks:
      - internal
      - dokploy-network

networks:
  internal:
    driver: bridge
  dokploy-network:
    external: true
